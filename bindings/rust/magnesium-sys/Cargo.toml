[package]
name = "magnesium-sys"
version = "1.0.0"
edition = "2021"
description = "Raw FFI bindings to the Magnesium language C API"
links = "magnesium"

[features]
default = ["vendored"]
vendored = []
system = []

[build-dependencies]
cc = "1"
pkg-config = "0.3"

[dependencies]
libc = "0.2"
